import "./App.css";
import React, { useEffect, useState } from "react";

const App = () => {
  const input = useRef(initialValue);
  return (
    <div className="App">
      <h1>Hello </h1>
    </div>
  );
};

export default App;
